[build]
  # Directory that contains the deploy-ready HTML files and assets generated by the build.
  publish = "."

  # Default build command. Since this is a static site, we don't need one.
  command = ""

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 404
  force = false
